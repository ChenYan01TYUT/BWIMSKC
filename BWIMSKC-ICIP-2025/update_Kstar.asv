function [Kstar] = update_Kstar(Z)%该函数接收一个输入参数 Z，它通常是一个节点的簇标签矩阵。输出是更新后的图谱矩阵 Kstar。

Z = (Z + Z')/2;%对输入的矩阵 Z 进行对称化处理。因为图谱矩阵应该是对称的（表示节点之间的相似度是对称的），所以这一步确保了 Z 的对称性。
[U, D] = eig(Z); %对对称矩阵 Z 进行特征值分解，得到U 是特征向量矩阵（每列是一个特征向量）。D 是对角矩阵，包含了矩阵 Z 的特征值。
D(find(D<0)) = 0;%这一步确保所有的特征值都是非负的。因为在图谱学习中，负的特征值可能表示不稳定的图结构（不符合图的定义）。因此，我们将所有小于 0 的特征值替换为 0，以确保图谱矩阵是有效的
Kstar = U*D*U';%通过重新组合特征向量和调整后的特征值来构造新的图谱矩阵 Kstar。
%U * D * U' 是将特征向量和特征值重新组合，得到新的图谱矩阵 Kstar。结果是一个对称矩阵，其中每个元素表示节点之间的相似度（或者说，图谱的加权关系）
end

